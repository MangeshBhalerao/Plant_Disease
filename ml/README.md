# ML

This folder will contain reusable machine learning code.

Use notebooks for exploration and `ml/` for repeatable scripts and modules.

Suggested responsibilities:

- data loading and preprocessing
- model training
- evaluation
- inference utilities

Suggested future structure:

```text
ml/
├── data/
├── training/
├── evaluation/
└── inference/
```
